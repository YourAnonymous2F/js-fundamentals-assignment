The First Assignment which is the Challenge1.js is all about  Creating variables to store my personal information and display them in formatted string. Requirements : Use const for values that won't change

The Second Assignment which is the Challenge2.js is Simple calculator that performs basic arithmetic operations on two numbers

The Third Assignment which is the Challenge3.js is a For Loop that checks whether numbers from 1-20 are even or odd while consoling the output as 1 is odd 2 is even 3 is odd ... 20 is even